name: Build and deploy to nick-graves.github.io

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "latest"
          extended: true

      - name: Show where we are + what Hugo sees
        run: |
          pwd
          ls -la
          echo "Hugo config files found:"
          find . -maxdepth 3 -type f \( -name "hugo.toml" -o -name "config.toml" -o -name "config.yaml" -o -name "config.yml" \)

      - name: Build
        run: hugo --minify --cleanDestinationDir

      - name: Deploy to root repo (nick-graves.github.io)
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.GH_PAGES_TOKEN }}
          external_repository: nick-graves/nick-graves.github.io
          publish_branch: main
          publish_dir: ./public
          commit_message: "Deploy Hugo site"